// ---------- EXPRESS BASE TEMPLATE ----------

// 1. Import express
const express = require("express");

// 2. Create an app
const app = express();

// 3. Add middleware to read JSON
app.use(express.json());

// 4. Define routes
app.get("/", (req, res) => {
  res.send("Server is running!");
});

//add routes and middleware below

// 5. Start the server on a port (change if needed)
const PORT = 3000;
app.listen(PORT, () => {
  console.log(`Server running at http://localhost:${PORT}`);
});


to run stuff now

npx nodemon server-practice/server_practice_.js





boilerplate


// Change the port number per file to avoid conflicts (e.g. 3000, 3001, etc)

const express = require("express");
const app = express();

// Middleware: allows Express to read JSON body data
app.use(express.json());

// 🧠 ROUTES GO BELOW

app.get("/", (req, res) => {
  res.send("Server Practice # is running!");
});

// ⚠️ Express 5 — catch-all 404 handler
app.use((req, res) => {
  res.status(404).send("Page not found");
});

// 🟢 Start the server
app.listen(3002, () => {
  console.log("Server Practice # running at http://localhost:3002");
});
